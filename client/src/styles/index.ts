import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    input: {
        padding: 12,
        paddingHorizontal: 15,
        borderWidth: 1,
        borderColor: "#ccc",
        borderRadius: 8,
        marginTop: 5,
        fontSize: 16
    }
})